#!/usr/bin/env bash
# The working directory during post_compile is $BUILD_DIR, which becomes /app at runtime
export PLAYWRIGHT_BROWSERS_PATH="$(pwd)/ms-playwright"
echo "-----> Installing Playwright Chromium browser to $(pwd)/ms-playwright"
python -m playwright install chromium
echo "-----> Chromium installation complete"
